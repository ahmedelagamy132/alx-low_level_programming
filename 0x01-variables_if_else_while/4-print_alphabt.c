#include <stdio.h>
/**
 * main - hfghgfh
 * Return: 0
 */

int main(void)
{
	char caracter = 'a';

	do {
	if (caracter != 'e' && caracter != 'q')
	{
	putchar (caracter);
	}
	caracter++;
	} while (caracter <= 'z');

	putchar('\n');

return (0);
}
