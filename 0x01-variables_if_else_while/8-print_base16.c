#include <stdio.h>
/**
 * main - hfghgfh
 * Return: 0
 */

int main(void)
{
	int numb = 0;
	char caracter = 'a';

	do {
	putchar (numb);
	numb++;
	} while (numb <= 9)

	do {
	putchar (caracter);
	caracter++;
	} while (caracter <= 'f');

	putchar ('\n');

return (0);

}
