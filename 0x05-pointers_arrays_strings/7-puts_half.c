#include "main.h"

/**
 * puts_half - lknblkgbm;lcbv
 * @str: lmbklcmvmkg
 */

void puts_half(cha;

	for (len = 0; str[len] != '\0'; len++)
		;
	if (len % 2 == 0)
	{
		for (n = (len / 2); n < len; n++)
			_putchar(
